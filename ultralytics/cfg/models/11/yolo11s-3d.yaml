# YOLOv11s 3D model
inherits: yolo11-3d.yaml

# Parameters
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.50  # layer channel multiple
scales:
  # [depth, width, max_channels]
  s: [0.33, 0.50, 1024]  # YOLOv11s-3D 